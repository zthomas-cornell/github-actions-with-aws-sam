on:
  push:
    branches:
      - dev
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - uses: aws-actions/setup-sam@v1
      - uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-west-2
      # sam build
      - run: sam build --use-container

# Run Unit tests- Specify unit tests here

# sam deploy
      - run: sam deploy --parameter-overrides Role=arn:aws:iam::324043197527:role/CornellLambdaRole Env=test --no-confirm-changeset --no-fail-on-empty-changeset --stack-name hello-planets --resolve-s3 --capabilities CAPABILITY_IAM --region us-west-2