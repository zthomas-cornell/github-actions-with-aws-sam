AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: >
  github-actions-with-aws-sam

  Sample SAM Template for github-actions-with-aws-sam

# https://github.com/awslabs/serverless-application-model/blob/master/docs/globals.rst
Globals:
  Function:
    Timeout: 180
    Runtime: python3.8
    Handler: app.lambda_handler
    Architectures:
        - x86_64

# https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/parameters-section-structure.html
Parameters:
  Role:
    Type: String
    Default: arn:aws:iam::324043197527:role/CornellLambdaRole
    AllowedValues:
      - arn:aws:iam::324043197527:role/CornellLambdaRole
      - arn:aws:iam::324043197527:role/CornellLambdaRole
    Description: The lambda role ARN.

  Env:
    Type: String
    Default: test
    AllowedValues:
      - test
      - test
    Description: The environment that the lambdas belong to.

# https://github.com/awslabs/serverless-application-model/blob/master/versions/2016-10-31.md#awsserverlessfunction
Resources:
  HelloEarthFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: hello_earth/
      Role: { "Ref" : "Role" }

  HelloMarsFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: hello_mars/
      Role: { "Ref" : "Role" }
